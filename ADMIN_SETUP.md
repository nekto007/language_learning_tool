# 🔐 Создание администратора приложения

## ⚠️ ВАЖНО: Изменения в безопасности

Начиная с этой версии, **автоматическое создание администратора с кредами `admin/password` УДАЛЕНО** из-за критической уязвимости безопасности.

Теперь для создания первого администратора используйте безопасный CLI-скрипт `create_admin.py`.

---

## 🚀 Способы создания администратора

### 1. Интерактивный режим (Рекомендуется)

```bash
python create_admin.py
```

Скрипт запросит:
- Username
- Email
- Пароль (с проверкой стойкости)
- Подтверждение пароля

**Пример:**
```
==============================================================
  СОЗДАНИЕ АДМИНИСТРАТОРА
==============================================================

Введите имя пользователя (username): admin
Введите email: admin@example.com

Требования к паролю:
  • Минимум 8 символов
  • Хотя бы одна заглавная буква
  • Хотя бы одна строчная буква
  • Хотя бы одна цифра
  • Рекомендуется специальный символ (!@#$%^&*)

Введите пароль: ************
Подтвердите пароль: ************

==============================================================
✅ Администратор успешно создан!
==============================================================
Username: admin
Email:    admin@example.com
Admin:    Yes
==============================================================
```

---

### 2. Через переменные окружения (Безопасно для CI/CD)

```bash
export ADMIN_USERNAME=admin
export ADMIN_EMAIL=admin@example.com
export ADMIN_PASSWORD='MySecureP@ssw0rd123!'
python create_admin.py
```

**Docker Compose пример:**
```yaml
services:
  web:
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_EMAIL=admin@example.com
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}  # Из .env файла
```

---

### 3. Через аргументы командной строки (Небезопасно!)

```bash
python create_admin.py --username admin --email admin@example.com --password 'MySecureP@ssw0rd123!'
```

⚠️ **НЕ РЕКОМЕНДУЕТСЯ**: Пароль может быть виден в истории команд и логах процессов.

---

## 🔒 Требования к паролю

Скрипт автоматически проверяет стойкость пароля:

### Обязательные требования:
- ✅ Минимум 8 символов
- ✅ Хотя бы одна **заглавная** буква (A-Z)
- ✅ Хотя бы одна **строчная** буква (a-z)
- ✅ Хотя бы одна **цифра** (0-9)
- ✅ Максимум 128 символов

### Рекомендуемые требования:
- ⭐ Специальные символы (!@#$%^&* и т.д.)

### Защита от слабых паролей:
- ❌ Нельзя использовать распространенные пароли (password, admin123, и т.д.)
- ❌ Пароль не должен совпадать с username
- ❌ Пароль не должен совпадать с email
- ❌ Нельзя использовать последовательности (abc, 123)
- ❌ Нельзя использовать повторяющиеся символы (aaaa, 1111)

---

## 📝 Примеры безопасных паролей

✅ **Хорошие пароли:**
- `MyApp2024!Secure`
- `Tr0ng#P@ssword`
- `Adm!n$ecur3Pass`

❌ **Плохие пароли:**
- `password` (слишком распространенный)
- `admin123` (слишком простой)
- `12345678` (только цифры)
- `abcdefgh` (только буквы)
- `aaaaaaaa` (повторяющиеся символы)

---

## 🛠️ Опции командной строки

```
python create_admin.py --help

Опции:
  -u, --username USERNAME   Имя пользователя
  -e, --email EMAIL         Email
  -p, --password PASSWORD   Пароль (небезопасно - используйте переменные окружения)
  -f, --force               Создать даже если админ уже существует
  -h, --help                Показать справку
```

---

## 🔄 Создание дополнительных администраторов

Если администратор уже существует, скрипт предупредит вас:

```
⚠️  Администратор уже существует: admin (admin@example.com)

Вы хотите создать еще одного администратора? (yes/no):
```

Или используйте флаг `--force`:
```bash
python create_admin.py --force
```

---

## 🐳 Docker и Production

### Пример для production-деплоя:

1. Создайте `.env` файл (НЕ коммитьте в git!):
```env
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@yourcompany.com
ADMIN_PASSWORD=YourVerySecurePassword123!
```

2. В Dockerfile добавьте:
```dockerfile
# После инициализации БД
RUN python create_admin.py
```

3. В docker-compose.yml:
```yaml
services:
  web:
    env_file:
      - .env
```

---

## ❓ FAQ

### Q: Что случилось с автоматическим созданием admin/password?
**A:** Это была критическая уязвимость безопасности. Злоумышленники могли войти с кредами `admin/password` по умолчанию.

### Q: Как создать первого администратора при первом запуске?
**A:** Используйте `python create_admin.py` в интерактивном режиме или через переменные окружения.

### Q: Можно ли использовать старые базы данных с admin/password?
**A:** Да, существующие пользователи останутся. Но рекомендуется сменить пароль через интерфейс приложения.

### Q: Как сбросить пароль существующего администратора?
**A:** Используйте функцию "Забыли пароль?" в интерфейсе или вручную через БД.

---

## 📚 Дополнительная информация

- **Валидация паролей**: `app/utils/password_validator.py`
- **Формы регистрации**: `app/auth/forms.py`
- **CLI скрипт**: `create_admin.py`

Все новые регистрации и сброс пароля теперь также используют валидацию стойкости паролей.