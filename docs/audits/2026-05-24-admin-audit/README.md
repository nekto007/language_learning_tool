# Admin full audit (2026-05-24)

Living findings journal for `docs/plans/2026-05-24-admin-full-audit.md`. Each task in the plan appends a short section here with the findings it discovered and the fix it applied.

## Snapshot artifacts (Task 1)

- `url-map.md` — every admin route (method, URL, view, module, wrapper chain) generated by `scripts/_audit_dump_admin_routes.py`.
- `inventory.md` — LOC inventory of `app/admin/`, services, templates, tests + missing-coverage list.

## Decorator baseline (Task 1)

- `admin_required` already existed in `app/admin/utils/decorators.py` and is the only admin gate in use. Task 1 normalises its docstring and adds `admin_audit_required(action_name, target_type_callable=None, target_id_callable=None)` — a thin wrapper that combines `admin_required` with a call to `log_admin_action()` on a successful 2xx response so destructive endpoints adopted in Tasks 2–4 get audit logging for free.
- `tests/admin/test_admin_url_inventory.py` enumerates every admin-prefixed route and asserts that anonymous / non-admin access redirects to login or returns 401/403/404. A short, whitelisted set of endpoints (e.g. the GSC OAuth callback) is documented inline.

## Findings — Task 1

1. Inventory revealed `app/admin/main_routes.py.backup` (3170 LOC) still in the tree — confirmed dead and queued for Task 7/27 deletion.
2. Two blueprints still expose overlapping curriculum routes (legacy `admin.*` view-functions in `app/admin/curriculum.py` and the new `admin_curriculum.*` blueprint in `app/admin/routes/curriculum_routes.py`). They register the same URLs (`/admin/curriculum/lessons`, `/admin/curriculum/levels`, `/admin/curriculum/modules`, `/admin/curriculum`, `/admin/curriculum/progress`, `/admin/curriculum/import`); the last-registered blueprint wins. Task 7 / Task 12 owns disambiguation.
3. `admin.search_words_api` and `admin.api_words_search` both bind to `/admin/api/words/search`. Same shadowing risk. Flagged for Task 27.
4. `curriculum_admin.*` (legacy curriculum admin under `/curriculum/admin/*`) is a separate prefix from the `admin_curriculum.*` sub-blueprint under `/admin/curriculum/*`. Both should be reconciled in Task 12.
5. Several sub-blueprints have no dedicated unit-tests (see `inventory.md` § "Missing coverage").

Future tasks should append a `## Findings — Task N` section here.

## Findings — Task 3 (CSRF coverage)

Static-analysis sweep of `app/templates/admin/**/*.html` for `<form method="POST">` and `fetch()` AJAX calls. CSRFProtect is enabled in production via `config/settings.py`; tests rely on template/script audits because `tests/conftest.py` disables CSRF (`WTF_CSRF_ENABLED = False`) for the rest of the suite.

Gaps found and fixed:

1. `app/templates/admin/books/add.html` — the inline "Перезапустить обработку слов" form (action `books.reprocess_book_words`) submitted POST without `csrf_token()`; added a hidden `csrf_token` input.
2. `app/templates/admin/book_courses/detail.html` — `toggleCourseStatus` and `toggleFeatured` built an empty `FormData()` for `/admin/book-courses/bulk-operations` without any CSRF marker. Added `csrf_token` to the body and `X-CSRFToken` header (token sourced from the existing `<meta name="csrf-token">` injected by `admin/base.html`).
3. `app/templates/admin/book_courses/edit.html` — same `toggleCourseStatus` pattern, same fix.

No admin route uses `@csrf.exempt`. `grep csrf.exempt app/admin/**/*.py` returns only documentation comments in `app/api/decorators.py` (out of scope).

Regression tests live in `tests/admin/test_task3_admin_audit_csrf.py`:

- `TestAdminTemplateCsrfCoverage.test_post_forms_have_csrf_token` — parametrized over every `app/templates/admin/**/*.html`; asserts each POST `<form>` carries one of `csrf_token()`, `form.hidden_tag()`, `form.csrf_token`, or `name="csrf_token"` before `</form>`.
- `TestAdminTemplateCsrfCoverage.test_ajax_mutating_calls_carry_csrf` — parametrized over the same templates; asserts every `fetch(...)` POST/PUT/DELETE/PATCH either sends `X-CSRFToken`, appends `csrf_token` to the body, or is given a `new FormData(<formEl>)` whose form is already covered by the previous test.
- `TestAdminRoutesNoCsrfExempt.test_no_csrf_exempt_in_admin_modules` — greps `app/admin/**/*.py` for `csrf.exempt` / `@csrf_exempt` outside comments.

