# Аудит: 100 багов и edge cases (только документирование)

## Overview

Системный аудит проекта с целью найти и задокументировать 100 багов / edge cases. Результат — единый структурированный реестр в `docs/audit/2026-06-13-100-edge-cases.md`. Правок кода НЕТ — только находки (severity, file:line, repro, предлагаемый фикс). Формат повторяет существующий `docs/audit/2026-05-23-audit-findings.md`.

## Context

- Output file: `docs/audit/2026-06-13-100-edge-cases.md` (новый)
- Образец формата: `docs/audit/2026-05-23-audit-findings.md`
- Зоны охвата (по CLAUDE.md): XP/Levels/Streaks, SRS, Daily Plan, Curriculum/Lessons/Grading, Books/Reading, Achievements/Race/Challenge, Notifications/Feedback, Admin, API/Security/Middleware, Infrastructure, Frontend/templates/CSS, контент модулей (JSON)
- Методика: чтение кода + grep по edge-сигнатурам (None/[]/0/boolean, naive vs aware datetime, race/idempotency, off-by-one, тайзоны, деление на ноль, флаги precedence), запуск `pytest` / `pytest -m smoke` для фиксации красных тестов, сверка с уже известными failing tests (memory: ~52 red на master)
- Severity: P0 (data-loss/security/сломанный сценарий), P1 (UX-баг, тех-долг с риском), P2 (полировка/dead code)

## Development Approach

- **Testing approach**: N/A — это аудит, кода не меняем, тестов не пишем. «Verify» в каждой задаче = подтвердить, что каждая находка ссылается на реальный `file:line` и воспроизводима по коду (grep/read), без ложных срабатываний.
- Каждая задача — sweep одного доменного кластера, дописывает батч находок в единый реестр с уникальными id (E-001…E-100+).
- Распределение квоты ~ по 12-15 находок на доменную задачу, чтобы суммарно перевалить за 100.
- Дубли и «не баг» отсекаются в финальной консолидации.

## Implementation Steps

### Task 1: Скелет реестра + базовая линия тестов

**Files:**
- Create: `docs/audit/2026-06-13-100-edge-cases.md`

- [x] Создать `docs/audit/2026-06-13-100-edge-cases.md`: заголовок, методика, легенда severity, описание колонок (`id | layer | severity | file:line | описание | repro | предлагаемый фикс`), пустые секции A–K по кластерам
- [x] Запустить `pytest -m smoke` и полный `pytest`, зафиксировать красные тесты; сверить с известным baseline (~52 pre-existing red) и вынести в секцию «A. Падающие тесты» только реальные баги, не baseline-шум (smoke 596 passed/2 skipped зелёный; full 44 failed — все в baseline-кластере, новых регрессий нет)
- [x] Verify: файл создан, секции присутствуют, baseline-тесты отделены от находок

### Task 2: XP, Levels, Streaks

**Files:**
- Modify: `docs/audit/2026-06-13-100-edge-cases.md`

- [ ] Аудит `app/achievements/xp_service.py`, `app/daily_plan/linear/xp.py`, `app/curriculum/xp.py`, `app/achievements/streak_service.py`, `app/utils/time_utils.py`: score-aware округление, идемпотентность helpers, dedup-ключи, user-local date / timezone fallback, streak multiplier cap, perfect-day bonus, paused-дни
- [ ] Записать находки E-xxx с file:line и repro
- [ ] Verify: каждая находка ссылается на реальный код

### Task 3: SRS + Daily Plan

**Files:**
- Modify: `docs/audit/2026-06-13-100-edge-cases.md`

- [ ] Аудит `app/srs/*` (counting, service, scheduling, constants) и `app/daily_plan/*` (plan, service, items/*, skips, challenge, next_step, linear/context): naive-UTC convention, day-anchored scheduling vs wall-clock, learning/relearning offset edge, leech burial, adaptive limits, deck-quiz подмена, day_secured race, optional accumulation/дедуп, slot-skip квоты
- [ ] Записать находки с repro
- [ ] Verify: находки воспроизводимы по коду

### Task 4: Curriculum, Lessons, Grading

**Files:**
- Modify: `docs/audit/2026-06-13-100-edge-cases.md`

- [ ] Аудит `app/curriculum/*` (navigation, security, grading, lessons.py, routes/*, constants): passing-score константы, strict grading (fill-in-blank Levenshtein, matching pairs, None options), final-test 24h-лимит и grading-persist failure, XP savepoint, pronunciation requires_attempt, reset=true пути, done-today score gating, grammar union read-time
- [ ] Записать находки
- [ ] Verify: находки воспроизводимы

### Task 5: Books & Reading

**Files:**
- Modify: `docs/audit/2026-06-13-100-edge-cases.md`

- [ ] Аудит `app/books/*` (reading_session, api, progress, access, vocab_pull) + `app/curriculum/book_courses.py`: completion threshold (0.99 vs 1.0), reading slot book-scoped done, vocab pull degenerate slice, open-session grace, per-book access / expiration UTC-базис, chapter-relative progress, is_published draft gating
- [ ] Записать находки
- [ ] Verify: находки воспроизводимы

### Task 6: Achievements, Race, Challenge, Notifications, Feedback

**Files:**
- Modify: `docs/audit/2026-06-13-100-edge-cases.md`

- [ ] Аудит `app/achievements/*` (services, ranks, daily_race, seed.py↔check соответствие), `app/notifications/services.py`, `app/feedback/*` (models, routes, storage): race-safe grant, seed-код vs check-список, immersion streak 4-skills, streak shield clear, feedback статусные переходы, screenshot validation, auto-context regex/cap, rate-limits
- [ ] Записать находки
- [ ] Verify: находки воспроизводимы

### Task 7: Admin, API, Security, Middleware, Infrastructure

**Files:**
- Modify: `docs/audit/2026-06-13-100-edge-cases.md`

- [ ] Аудит `app/admin/*`, `app/api/errors.py`, `app/__init__.py` (error JSON handlers, settings TTL cache), `app/middleware/*`, `app/utils/*` (file_security, validators, db_utils), `app/health.py`, `app/email_scheduler.py`, `app/telegram/routes.py`, `app/reminders/*`: DAU/WAU UNION источники, CSV sanitize, redirect safety, magic-bytes upload, CSP nonce, request-id, rate-limit storage per-worker, webhook dedup/fail-closed, scheduler advisory-lock, delivery window timezone
- [ ] Записать находки
- [ ] Verify: находки воспроизводимы

### Task 8: Frontend, templates, CSS, контент

**Files:**
- Modify: `docs/audit/2026-06-13-100-edge-cases.md`

- [ ] Аудит `app/templates/**` и `app/static/js/**`: layout split (base vs public_base), adaptive breakpoints, innerHTML vs textContent (XSS), prefers-reduced-motion guards, reader progress формула, lesson fetchNextSlot/standalone fallback, notifications dropdown edge; плюс контент-edge модулей JSON (None options, пустые pairs, квоты reading)
- [ ] Записать находки
- [ ] Verify: находки воспроизводимы

### Task 9: Консолидация и верификация реестра

**Files:**
- Modify: `docs/audit/2026-06-13-100-edge-cases.md`

- [ ] Дедуп находок, присвоить итоговые severity, перенумеровать id сквозняком
- [ ] Подтвердить, что суммарно ≥100 уникальных багов/edge cases; если меньше — добрать дополнительным sweep'ом по самым «горячим» кластерам (XP/SRS/daily_plan)
- [ ] Добавить сводную таблицу по severity и по слоям + раздел «Топ-риски P0»
- [ ] Verify: счётчик ≥100, нет дублей, каждая запись имеет file:line и repro

### Task 10: Update documentation

**Files:**
- Modify: `CLAUDE.md` (опционально)

- [ ] Обновить `CLAUDE.md` при необходимости (если аудит вскрыл расхождение паттернов с реальным кодом — отметить ссылкой на реестр; иначе пропустить)

## Post-Completion

- Реестр `docs/audit/2026-06-13-100-edge-cases.md` содержит ≥100 уникальных находок с severity, file:line, repro и предлагаемым фиксом.
- Сводные таблицы по severity и по слоям, раздел «Топ-риски P0».
