# Постоянное улучшение: сквозной аудит 4 зон + ремедиация

## Overview

Свежий многоагентный аудит четырёх зон продукта — **UI**, **контент**, **разделы (blueprints/флоу)**, **админка** — с адверсариальной верификацией каждой находки, консолидацией в единый реестр `docs/audit/2026-08-08-cross-zone-audit.md`, и последующей ремедиацией по severity (P0 → P1 → отобранные P2).

Формат реестра и процесс копируют уже работающую в проекте схему двух предыдущих аудитов (`2026-06-13-100-edge-cases.md`, `2026-06-19-daily-plan-lesson-frontend-audit.md`): таблица находок с ID, severity-сводка, секция «Статус ремедиации» с коммитами.

Принцип: **аудит не правит код**, ремедиация не переоткрывает аудит. Между ними — гейт консолидации, где находки дедуплицируются и перепроверяются скептиками; в реестр попадают только CONFIRMED.

## Context

**Зона UI**
- `app/templates/**/*.html` (265 шаблонов), `app/templates/base.html` / `public_base.html`
- `app/static/css/design-system.css` (~19 257 строк)
- `app/static/js/` — `lesson-completion.js`, `linear-plan-context.js`, `linear-daily-plan.js`, `flashcard-session.js`, `daily-plan-next.js`, `unified-js.js`, подкаталоги `curriculum/`, `books/`, `study/`, `words/`, `grammar/`
- Спеки: `docs/design/lesson-frontend-spec.md`, `docs/design/lesson-completion-contract.md`, `docs/design/header-footer-inventory.md`

**Зона Контент**
- `content/` — 86+ модулей JSON (**gitignored**: правки доставляются скриптами в `scripts/`, не коммитом данных)
- `scripts/validate_corpus.py`, `scripts/validate_module_completed_json.py`, `scripts/diff_module_json_against_db.py`, семейство `scripts/audit_*.py`
- Контракт: `docs/design/module-source-json-contract.md`
- Известный контекст: аудит 2026-06-18 (86 модулей, дефекты bulk-генератора) закрыт проходами 1–7, `validate_corpus.py` = 0 errors / 0 warnings; открытый хвост — TTS-реген ~11 уроков + реимпорт (ручной, вынесен в Post-Completion)

**Зона Разделы**
- Blueprints: `achievements`, `admin`, `api`, `auth`, `books`, `curriculum`, `courses`, `feedback`, `grammar_lab`, `health_check`, `landing`, `legal`, `modules`, `notifications`, `onboarding`, `race`, `reminders`, `seo`, `study`, `telegram`, `words`
- Гейтинг: `app/modules/decorators.py`, `app/curriculum/security.py`, `app/books/access.py`
- Ошибки/редиректы: `app/api/errors.py`, `app/__init__.py` (handle_403/404/500), `app/auth/routes.py::get_safe_redirect_url`

**Зона Админка**
- `app/admin/routes/` — 19 файлов, `app/admin/services/`, `app/templates/admin/`
- `app/admin/audit.py` (`log_admin_action`, `admin_audit_required`), `app/admin/utils/request_validators.py`, `app/admin/site_settings.py`
- Известный контекст: content-quality dashboard показывает только coverage-метрики (audio/IPA/examples), не классы дефектов генератора

**Открытые хвосты предыдущего аудита (входят в стартовый seed реестра, не переоткрываются заново)**
- Stage 4 dead-code: `daily-plan-next.js`, легаси `#lesson-footer` observer, `showLessonCompletion` shim, SRS observer
- JS i18n routing, a11y-хвосты (focus management, `aria-valuenow`, textContent vs innerHTML)
- `final_test` hidden-form POST без обработки ошибок, `writing_prompt` reload state

**Тестовая база**
- `pytest` (449 test-файлов), `pytest -m smoke` (~70 тестов, <30 с)
- Известный красный baseline на master: ~44–52 падения. **Любая оценка «зелено/красно» — только диффом против зафиксированного baseline**, не по абсолютному числу
- Линтер: `ruff` (py313, line-length 120, конфиг в `pyproject.toml`)

## Development Approach

- **Testing approach**: Regular (код, затем тесты) — кроме исправлений P0, где сначала пишется падающий регресс-тест, затем фикс
- Аудитные задачи (1–6) кода **не меняют** — только пишут в `docs/audit/2026-08-08-cross-zone-audit.md`
- Многоагентный паттерн: на каждую линзу — свой финдер; каждая находка перепроверяется независимым скептиком, настроенным опровергать; в реестр идут только CONFIRMED, PLAUSIBLE — в приложение
- Ремедиация — инкрементальными коммитами по группам находок, сообщения без упоминаний AI
- **CRITICAL: каждая ремедиационная задача ОБЯЗАНА включать новые/обновлённые тесты**
- **CRITICAL: перед переходом к следующей задаче — прогон `pytest`, ноль новых падений против baseline**
- Границы объёма: чинятся все P0 и P1 + те P2, что самодостаточны и низкорисковы. P3 и рискованные P2 остаются в реестре как открытые, с явным списком в финальной сводке
- YAGNI: не вводить новые абстракции, слои совместимости и «на будущее» — только то, что закрывает конкретную находку

## Implementation Steps

### Task 1: Каркас аудита и фиксация базовой линии

**Files:**
- Create: `docs/audit/2026-08-08-cross-zone-audit.md`
- Create: `docs/audit/2026-08-08-baseline-pytest.txt`

- [x] Прогнать полный `pytest`, сохранить список падающих nodeid в `docs/audit/2026-08-08-baseline-pytest.txt` (это эталон «известно-красного») — 9908 собрано, **27 FAILED**, 0 errors; рецепт сверки проверен round-trip'ом
- [x] Прогнать `ruff check .`, зафиксировать текущее число нарушений в том же файле отдельной секцией — **4582** (ruff 0.5.6 из кэша pre-commit; разбивка по каталогам + per-rule)
- [x] Создать реестр `docs/audit/2026-08-08-cross-zone-audit.md` по образцу `2026-06-19-daily-plan-lesson-frontend-audit.md`: секции «Как собрано», «Сводка severity», по секции на зону (UI / Контент / Разделы / Админка), «Приложение: PLAUSIBLE», «Статус ремедиации»
- [x] Зафиксировать в реестре схему находки: `ID | Зона | Файл:строка | Severity | Симптом | Сценарий отказа | Верификация` и критерии P0–P3 (P0 = сломанный пользовательский путь/утечка данных; P1 = функциональный баг с обходом; P2 = деградация UX/производительности; P3 = косметика/долг)
- [x] Засеять реестр открытыми хвостами аудита 2026-06-19 (Stage 4 dead-code, JS i18n, a11y-хвосты, `final_test` POST, `writing_prompt` reload) со статусом «унаследовано, требует перепроверки» — 13 находок `INH-001…INH-013`, все пути и коммит-SHA сверены с текущим деревом

### Task 2: Аудит зоны UI

**Files:**
- Modify: `docs/audit/2026-08-08-cross-zone-audit.md`
- Read-only: `app/templates/**/*.html`, `app/static/css/design-system.css`, `app/static/js/**`

- [x] Фан-аут финдеров по линзам: (а) доступность — focus management, `aria-*`, роли, контраст, textContent vs innerHTML; (б) fetch-надёжность — отсутствующие `resp.ok`, необработанные reject, отсутствие loading/disabled-состояний; (в) i18n — хардкод русских строк в JS/шаблонах в обход `_()` и `window.I18N`
- [x] Фан-аут финдеров по линзам: (г) адаптив — соответствие брейкпоинтам 1200 / 992 из CLAUDE.md, overflow, touch-таргеты; (д) корректность layout split — `public_base.html` vs `base.html` по типу страницы, инлайновые `<style>` в обход `design-system.css`; (е) мёртвый код — неиспользуемые CSS-классы, неподключённые JS-файлы, осиротевшие шаблоны
- [x] Перепроверить каждую находку независимым скептиком (задача — опровергнуть; при сомнении → refuted); подтверждать по факту чтения кода, а не по правдоподобию — 45 находок → 30 CONFIRMED / 15 REFUTED / 0 PLAUSIBLE; P1-претензии дополнительно проверены вручную (`change_password.html` и `books/add.html` отсутствуют, `extra_js` не объявлен в `admin/base.html`, двойной рендер `.word-checkbox`, отсутствие глобального `_` в JS, 0 ссылок на 5 JS / 9 CSS / 5 шаблонов-сирот)
- [x] Дедуплицировать внутри зоны, записать CONFIRMED в секцию «UI» реестра с ID `UI-NNN`, PLAUSIBLE — в приложение — **39 находок `UI-001…UI-039`** (0 P0 / 7 P1 / 10 P2 / 22 P3); кросс-лензовых дублей по `file:line` — 0, схлопнута одна пара `C06`+`INH-008` → `UI-022`; приложение PLAUSIBLE — 7 непроверенных скептиком кандидатов (`PL-UI-01…07`)
- [x] Зафиксировать в реестре, какие подзоны были просканированы, а какие сознательно не покрывались (без молчаливых усечений) — таблица «Покрытие» + секция «Критик-агент на полноту» с несмягчённым списком непрочитанного и утверждений, стоящих на скрипте, а не на чтении
- [x] Бонус (контракт реестра из Task 1): перепроверены все 13 унаследованных хвостов `INH-001…INH-013` — 10 CONFIRMED промотированы в `UI-NNN`, 3 REFUTED (`INH-002`, `INH-011`, `INH-012`) закрыты на месте; разрешено самопротиворечие исходного реестра по `final_test`

### Task 3: Аудит зоны «Контент»

**Files:**
- Modify: `docs/audit/2026-08-08-cross-zone-audit.md`
- Read-only: `content/**`, `scripts/validate_corpus.py`, `scripts/diff_module_json_against_db.py`, `docs/design/module-source-json-contract.md`

- [x] Прогнать существующие валидаторы (`validate_corpus.py`, `validate_module_completed_json.py`) и зафиксировать актуальный результат в реестре — это точка отсчёта, а не находки — **прогнаны 4** (+ `audit_module_completed_json_gaps.py`): `validate_corpus` = 0 errors / 0 warnings; `validate_module_completed_json` = 29 errors (**все ложные — `CNT-012`**); gap-аудитор = 1 canonical / 85 needs-work
- [x] Прогнать `diff_module_json_against_db.py`: расхождения JSON↔БД (уроки, изменённые в JSON, но не переимпортированные) — каждое расхождение в реестр — DB-ветка скрипта **сломана** (`CNT-013`), прогнано обходом: 86/86 модулей, 1548/1548 уроков по `external_key`, 0 json-only / 0 db-only / 0 коллизий; сверх скрипта — посимвольная сверка `content` всех 1548 уроков: **содержательный дрейф 0** (`CNT-000`), реимпорт не требуется
- [x] Фан-аут финдеров по линзам: (а) остатки дефектов bulk-генератора; (б) непроходимые/вырожденные упражнения; (в) покрытие медиа и метаданных — линзы (а) и (в) — отдельными агентами, (б) — детерминированной симуляцией грейдеров по всем 5 контейнерам + `test_sections` и полным перебором перестановок 1078 `ordering`
- [x] Фан-аут финдеров: (г) целостность прогрессии — `grammar_topic_id` связи, соответствие уровня CEFR содержанию, prerequisites модулей, доступность спайна без тупиков — исполнено против живой `learn_db_prod`; ключевое: `CNT-005` (защита уровней отсутствует в данных), `CNT-014` (формат prerequisites не парсится)
- [x] Верифицировать находки скептиками, записать CONFIRMED как `CNT-NNN`; для каждой указать модуль/урок и способен ли фикс быть выражен скриптом в `scripts/` — **33 находки `CNT-001…CNT-033`** (0 P0 / 9 P1 / 13 P2 / 11 P3) + таблица «Выразим ли фикс скриптом» + 8 `PL-CNT-*` в приложении. Скептик поправил `CNT-001` (обработчиков финального теста два, сломан только путь первой попытки); две претензии финдеров понижены P1→P3 после проверки первоисточника (`CNT-024`, `CNT-030`), семь опровергнуто

### Task 4: Аудит зоны «Разделы»

**Files:**
- Modify: `docs/audit/2026-08-08-cross-zone-audit.md`
- Read-only: `app/*/routes*.py`, `app/api/`, `app/modules/decorators.py`, `app/curriculum/security.py`, `app/books/access.py`

- [x] Инвентаризовать все зарегистрированные routes (через `app.url_map`) и сопоставить с шаблонами/линками: недостижимые роуты, ссылки на несуществующие endpoint'ы, `url_for` с неверными аргументами — снимок **531 правила** / 52 неймспейса + резолюция **1 118** вызовов `url_for`: **7 несуществующих эндпоинтов** (все подтверждены живым `BuildError`), **0** вызовов с неверными аргументами (9 кандидатов опровергнуты как артефакт regex'а); претензия на «недостижимые роуты» снята целиком — метод признан ненадёжным и это записано в реестр, а не замолчано
- [x] Фан-аут финдеров по линзам: (а) авторизация и гейтинг — mutating-роуты без проверки владельца, обходы `@module_required` / `check_module_access` / `can_user_access_book`, IDOR; (б) контракт ошибок — ad-hoc dict вместо `api_error`, расхождения формы JSON-ответа, необработанные `next`-редиректы мимо `get_safe_redirect_url` — AST-скан всех **240** мутирующих роутов и **28** id-роутов; ключевое: `SEC-001` (три эндпоинта `api_books` мимо `can_user_access_book` — 1 803 задачи по 11 `companion_only` книгам доступны 25 юзерам без модуля), `SEC-002` (`?from=` в `href` мимо `get_safe_redirect_url`), `SEC-007` (240 ad-hoc JSON-ошибок в 32 файлах)
- [x] Фан-аут финдеров по линзам: (в) производительность — N+1 в листингах, отсутствие `chunk_ids` на больших `IN()`, некешируемые тяжёлые виджеты; (г) целостность транзакций — commit внутри хелперов вместо caller-commits, отсутствие savepoint вокруг XP-блоков, пути, где исключение оставляет частичную запись; (д) пустые состояния и тупики — экраны без контента и без CTA — прогнаны, находок не дали: 188 сайтов `.in_(...)` (пороги замерены на `learn_db_prod`: макс. колода 414, макс. `user_words` 810 при пороге 1000 → `PL-SEC-01`), все XP-блоки проверены на savepoint (`R6` — инвариант соблюдён), 8 листинговых шаблонов на empty-state. Неполнота линз (в)/(г)/(д) выписана в «Покрытие» без смягчения
- [x] Верифицировать скептиками, записать CONFIRMED как `SEC-NNN` с указанием blueprint'а и конкретного сценария отказа — **8 находок `SEC-001…SEC-008`** (0 P0 / 3 P1 / 0 P2 / 5 P3), все CONFIRMED чтением кода + живым `BuildError` + запросами к `learn_db_prod`; **8 претензий опровергнуто** (`R1…R8`), 4 не доведены до CONFIRMED и вынесены в `PL-SEC-01…04`
- [x] Отметить находки, пересекающиеся с реестром `2026-06-13-100-edge-cases.md` (все 102 закрыты) — регресс помечать явно как регресс — **регрессов 0**: таблица сверки в реестре; `E-047`/`E-048`/`E-050` (права на книги) и `E-078` (rollback в `admin_audit_required`) перепроверены — закрыты и держатся, `SEC-001` бьёт по другому семейству роутов, которое ими не покрывалось

### Task 5: Аудит зоны «Админка»

**Files:**
- Modify: `docs/audit/2026-08-08-cross-zone-audit.md`
- Read-only: `app/admin/**`, `app/templates/admin/**`

- [x] Инвентаризовать все admin-роуты по 19 sub-blueprint'ам: метод, декораторы, наличие `admin_audit_required` / `log_admin_action` — снимок **197 правил** в 20 неймспейсах (19 sub-blueprint'ов + `reminders` под `/admin/reminders`), **115 мутирующих**; AST-разбор **193** route-функций с цепочками декораторов и телами; таблица покрытия и 14 дублирующихся URL (победитель разрешён живым `url_map.match()`) — в реестре
- [x] Фан-аут финдеров по линзам: (а) покрытие аудит-логом — мутирующие роуты без записи в `AdminAuditLog`; (б) валидация ввода — параметры в обход `get_int_arg`/`get_enum_arg`/`get_choice_arg`/`escape_like`, необработанные 400; (в) экспорт и загрузки — санитизация CSV, лимиты строк, magic-bytes проверки файлов — ключевое: `ADM-002` (гейт покрытия слеп к 5 из 20 неймспейсов; 7 мутирующих роутов без аудита, из них 3 публикуют в канал и 1 шлёт массовый email), `ADM-007` (5 из 7 экспортов без `log_admin_action`, включая экспорт самого аудит-лога), `ADM-008` (валидаторы в 2 из 19 модулей, 4 живых `ilike` без `escape_like`). Претензии по санитизации CSV и загрузкам файлов **опровергнуты** (`RA2`, `RA4`)
- [x] Фан-аут финдеров по линзам: (г) пробелы операционных инструментов — чего не хватает в content-quality dashboard, чтобы видеть классы дефектов из зоны «Контент», а не только coverage-метрики; (д) админ-UI — мёртвые страницы, битые фильтры/пагинация, N+1 в листингах — `ADM-003` (дашборд не показывает ни одну из 33 находок зоны Контент; `ipa_pct`/`examples_pct` структурно нулевые у 16 типов из 17), `ADM-004` (4 редактора уроков без точки входа), `ADM-005` (1548 строк без пагинации + ленивый `lesson.module.level`), `ADM-006` (6 перекрытых роутов). Претензия «пагинация теряет фильтры» опровергнута (`RA3`); хенд-офф `SEC-008` закрыт (`RA5` — страницы мёртвые, не потерянные)
- [x] Верифицировать скептиками, записать CONFIRMED как `ADM-NNN` — **8 находок `ADM-001…ADM-008`** (0 P0 / 1 P1 / 4 P2 / 3 P3), все CONFIRMED чтением кода + живым `url_map.match()` + запросами к `learn_db_prod` + обходом файловой системы; **5 претензий опровергнуто** (`RA1…RA5`), 2 не доведены до CONFIRMED и вынесены в `PL-ADM-01…02`. Неполнота линз (в)/(д) выписана в «Покрытие» без смягчения
- [x] Для линзы (г) сформулировать находки как конкретные недостающие метрики/представления, а не как «сделать дашборд лучше» — `ADM-003` содержит таблицу из 6 недостающих метрик; у каждой указан источник данных, закрываемая находка `CNT-*` и число, которое она показала бы сегодня (напр. 1570 из 1580 битых ссылок на аудио слов при рапортуемом `missing_audio_count = 0`)

### Task 6: Консолидация реестра и приоритизация

**Files:**
- Modify: `docs/audit/2026-08-08-cross-zone-audit.md`

- [x] Кросс-зонная дедупликация: одна первопричина, проявляющаяся в нескольких зонах, схлопывается в одну находку со списком мест — схлопнуты **2 пары**: `SEC-005` → `CNT-001` (два разошедшихся обработчика финального теста; оба дефекта только в достижимом — проверено чтением обоих, `lesson_by_id` живёт на blueprint'е `learn`, поэтому `curriculum.lesson_by_id` не строится) и `UI-034` → `UI-031` (единственный слушатель `dailyPlanStepComplete` — `daily-plan-next.js:63`; 4 контейнера, 6 диспатчеров, 3 подключения; единственный эмиттер `from=daily_plan` — `phrase_review.py:207`, чей шаблон скрипт не подключает). `CNT-001` перезонирована Контент → Разделы. **88 → 86.** Четыре рассмотренные и НЕ схлопнутые пары записаны с причиной
- [x] Второй проход верификации по всем P0/P1-кандидатам: три независимых скептика с разными линзами (корректность / воспроизводимость / влияние на пользователя); понижать severity при большинстве опровержений — все **20** кандидатов прогнаны по трём линзам, таблица вердиктов в реестре: **4 понижения** (`CNT-004` → P3 — инструмент разработчика, поведения не меняет; `UI-006` → P2 — геометрия пересчитана по `design-system.css:8396-8445`, полное перекрытие только при вырезе >5px; `CNT-006`/`CNT-007` → P2 — шум без функционального отказа), **0 повышений**, два спора Task 3 (`CNT-001`, `CNT-002`) разрешены **в пользу P1**, кандидат на P0 (`SEC-001`) рассмотрен и отклонён с записанным основанием. ⚠️ Проход выполнен **одним проверяющим с тремя линзами**, не тремя агентами — ограничение и его цена выписаны в реестре, а не замолчаны
- [x] Заполнить «Сводку severity» (таблица P0–P3 + всего) и сформулировать корневые темы — повторяющиеся паттерны, а не список симптомов — итог **0 P0 · 16 P1 · 31 P2 · 39 P3 = 86**; найдена и исправлена арифметическая ошибка сводки Task 3 (индекс Контента даёт 14 P2 / 10 P3, а не 13/11); сходимость проверена скриптом по всем индексам. **5 кросс-зонных корневых тем**, каждая — с адресом правки, а не перечнем симптомов
- [x] Составить план ремедиации внутри реестра: список ID для Task 7 (все P0), Task 8 (все P1), Task 9 (отобранные P2) и явный список отложенного с причиной — Task 7 = **пусто** (P0 нет, вердикт с обоснованием, закрывается как no-op), Task 8 = **16 P1 + `CNT-004`** как предусловие приёмки (`CNT-004` → `CNT-003` строго в этом порядке), Task 9 = **28 ID**, отложено = **41 ID** по 8 группам причин. Сходимость `17 + 28 + 41 = 86` проверена построчно. Разрешён каждый из 4 унаследованных dead-code пунктов плана: удаляем только `daily-plan-next.js`, `#lesson-footer` observer и `showLessonCompletion` shim снимать **нельзя** (несущие), SRS-обзёрвер — чиним гонку, а не удаляем
- [x] Прогнать критик-агент на полноту: какая линза не запускалась, какое утверждение не проверено, какой файл не прочитан — результат добавить в реестр отдельной секцией — секция «Критик-агент на полноту (весь аудит)»: **6 линз не запускались ни в одной зоне** (CSRF, rate-limiting, CSP/заголовки, фоновые процессы, миграции, здоровье тестовой базы), 6 утверждений приняты без прямой проверки (ни одного рендера в браузере, ни одного экранного диктора, ни одного замера рантайма за весь аудит), обнаружен **несостоявшийся хенд-офф** между Task 2 и Task 5 (админские шаблоны с `fetch(` — каждая задача считала их чужими), перечислены непрочитанные подсистемы. Практическое следствие внесено в «Verification»: «0 errors» ложно-зелёных гейтов не засчитывается как доказательство
- [x] Гейт задачи: код не менялся (правится только реестр); `pytest -m smoke` — **694 passed, 0 failed**

### Task 7: Ремедиация P0

**Files:**
- Modify: файлы по списку P0 из реестра
- Modify: `docs/audit/2026-08-08-cross-zone-audit.md`
- Create: тесты в соответствующих подкаталогах `tests/`

- [ ] На каждую P0-находку сначала написать падающий регресс-тест, воспроизводящий сценарий отказа из реестра
- [ ] Исправить каждую P0, соблюдая конвенции проекта (`api_error`, caller-commits, savepoint вокруг XP, `PASSING_SCORE_*` из constants, `_()` для строк)
- [ ] Убедиться, что ранее падавшие регресс-тесты проходят
- [ ] Отметить в «Статусе ремедиации» реестра: что сделано, как, какой ID закрыт
- [ ] Прогнать полный `pytest` и сравнить с `docs/audit/2026-08-08-baseline-pytest.txt` — ноль новых падений

### Task 8: Ремедиация P1

**Files:**
- Modify: файлы по списку P1 из реестра
- Modify: `docs/audit/2026-08-08-cross-zone-audit.md`
- Create/Modify: тесты в `tests/`

- [ ] Сгруппировать P1 по зонам и чинить группами, коммитя инкрементально
- [ ] Исправить P1 зоны UI и зоны «Разделы»; на каждую — тест (для UI-находок без серверной поверхности — тест на рендер шаблона/наличие атрибута)
- [ ] Исправить P1 зоны «Админка»; на каждую — тест (аудит-лог, валидация ввода, права доступа)
- [ ] Исправить P1 зоны «Контент»: правки доставлять скриптом в `scripts/`, после прогона повторно прогнать `validate_corpus.py` — 0 errors
- [ ] Обновить «Статус ремедиации» реестра по каждому закрытому ID
- [ ] Прогнать полный `pytest`, диффом против baseline — ноль новых падений

### Task 9: Ремедиация отобранных P2

**Files:**
- Modify: файлы по списку P2 из Task 6
- Modify: `docs/audit/2026-08-08-cross-zone-audit.md`
- Create/Modify: тесты в `tests/`

- [ ] Исправить P2 из утверждённого в Task 6 списка (самодостаточные, низкорисковые), включая унаследованный dead-code cleanup: `daily-plan-next.js`, легаси `#lesson-footer` observer, `showLessonCompletion` shim, SRS observer — перед удалением каждого файла grep всех импортов, 0 ссылок
- [ ] Закрыть a11y-хвосты и JS i18n routing из унаследованных находок, если они подтвердились в Task 2
- [ ] Написать тесты на каждую правку; для удалений — тест, что затронутый флоу по-прежнему работает
- [ ] Отметить в реестре отложенные P2/P3 с причиной отказа от правки
- [ ] Прогнать полный `pytest`, диффом против baseline — ноль новых падений

### Task 10: Verify acceptance criteria

- [ ] Прогнать полный `pytest`, сравнить с `docs/audit/2026-08-08-baseline-pytest.txt` — ноль новых падений
- [ ] Прогнать `pytest -m smoke` — все зелёные
- [ ] Прогнать `ruff check .` — ноль новых нарушений против зафиксированного в Task 1 числа
- [ ] Прогнать `python -c "import app"` и `validate_corpus.py` — импорт чист, 0 errors в корпусе
- [ ] Проверить, что каждая находка в реестре имеет финальный статус (✅ закрыто / 🟡 частично / ⬜ открыто) и ни одна не осталась без вердикта

### Task 11: Update documentation

- [ ] Обновить `CLAUDE.md`: секция «Known issues / аудит» — ссылка на `docs/audit/2026-08-08-cross-zone-audit.md` и статус ремедиации
- [ ] Обновить `CLAUDE.md` в затронутых разделах Key Patterns, если ремедиация изменила внутренние контракты
- [ ] Обновить `docs/design/lesson-frontend-spec.md` / `lesson-completion-contract.md`, если Task 9 удалил механизмы, которые там описаны
- [ ] Обновить `README.md`, только если появились пользовательски заметные изменения

## Post-Completion (ручные шаги, вне автоматизируемых задач)

- TTS-регенерация ~11 изменённых уроков и реимпорт через админку — требует внешнего TTS и админ-сессии
- Реимпорт модулей, для которых Task 3 выявил JSON↔БД drift
- Визуальная проверка UI-правок в браузере на mobile / 992 / 1200 / desktop
- Деплой: push в master → на сервере `docker compose up -d --build`
